class Student{
    constructor({studentId, userId, defaultPageAllocation, pageBalance}){
        this.studentId = studentId;
        this.userId = userId;
        this.defaultPageAllocation = defaultPageAllocation;
        this.pageBalance = pageBalance;
    }
}

export default Student;